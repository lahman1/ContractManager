# Server Configuration
PORT=4000

# No additional configuration needed!
# SQLite database will be created at: ./db/contacts.sqlite3
# NeDB databases will be created at: ./db/preferences.db and ./db/notes.db
